name: Build and Deploy ASP.NET Core and Blazor Server App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0'

      - name: dotnet build Backend (ASP.NET Core)
        run: |
          cd RustDetector.api
          dotnet restore
          dotnet build --configuration Release

      - name: dotnet publish Backend (ASP.NET Core)
        run: |
          cd RustDetector.api
          dotnet publish RustDetector.api.csproj -c Release -o ./bin/Release/net8.0/publish

      - name: Upload backend artifact for deployment
        uses: actions/upload-artifact@v2
        with:
          name: backend-artifact
          path: ./RustDetector.api/bin/Release/net8.0/publish

      - name: dotnet build Frontend (Blazor Server App)
        run: |
          cd RustDetector.blazor
          dotnet restore
          dotnet build --configuration Release

      - name: dotnet publish Frontend (Blazor Server App)
        run: |
          cd RustDetector.blazor
          dotnet publish RustDetector.blazor.csproj -c Release -o ./bin/Release/net8.0/publish

      - name: Upload frontend artifact for deployment
        uses: actions/upload-artifact@v2
        with:
          name: frontend-artifact
          path: ./RustDetector.blazor/bin/Release/net8.0/publish

  deploy:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Production'

    steps:
      - name: Download Backend artifact
        uses: actions/download-artifact@v2
        with:
          name: backend-artifact

      - name: Deploy Backend to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'rustdetector'
          slot-name: 'Production'
          package: ./RustDetector.api/bin/Release/net8.0/publish
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_D3196DCE27274553A3C438185ECE00E7 }}

      - name: Download Frontend artifact
        uses: actions/download-artifact@v2
        with:
          name: frontend-artifact

      - name: Deploy Frontend to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'rustdetector'
          slot-name: 'Production'
          package: ./RustDetector.blazor/bin/Release/net8.0/publish
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_D3196DCE27274553A3C438185ECE00E7 }}
